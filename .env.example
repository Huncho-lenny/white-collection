# Supabase — Settings → API
VITE_SUPABASE_URL=https://tytscdansburphlhyhds.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-jwt-here

# One-time DB access for `npm run db:fix-rls` (Settings → Database → password)
# Never commit real values. Do not expose service_role in the frontend.
# SUPABASE_DB_PASSWORD=your-database-password
